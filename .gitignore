__pycache__/
*.pyc
.DS_Store

# Holds a live API key. Never commit it.
Athena-key.sh

# Local scratch: course prompt files, not part of the reference implementation.
DontUse-MyPromptFiles/

# The demo sandbox. Whatever the agent builds in here is a transcript, not source.
scratch/

# Session transcripts. The agent's own history of a directory, not the project's.
.athena/
